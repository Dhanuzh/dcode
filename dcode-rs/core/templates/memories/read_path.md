## Memory

You have read-only access to a memory folder from prior runs. Use it when the query relates to workspace history, conventions, or prior decisions. Skip for self-contained requests.

Memory layout:
- {{ base_path }}/MEMORY.md (searchable registry)
- {{ base_path }}/rollout_summaries/ (per-rollout recaps)
- {{ base_path }}/skills/ (skill folders)

Quick pass: Skim MEMORY_SUMMARY below, search MEMORY.md with relevant keywords, open 1-2 most relevant files if needed. Keep to ≤4 search steps.

If relying on unverified memory, note it may be stale.

========= MEMORY_SUMMARY BEGINS =========
{{ memory_summary }}
========= MEMORY_SUMMARY ENDS =========
